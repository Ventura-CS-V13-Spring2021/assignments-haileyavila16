#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

main()
{
  int rdnum;
  srand(time(0));
  for (int i; i=<99;i++){
    rand_num = (rand() % 99) + 1;
    cout << rdnum << endl;
  }

}
