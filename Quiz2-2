#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

main()
{
  srand((unsigned) time(0));
  int rand_num;
  for (int i=0; i<10;i++){
    rand_num = (rand() % 99) + 1;
    cout << rand_num << endl;
  }

}
